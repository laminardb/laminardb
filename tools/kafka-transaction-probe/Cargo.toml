[package]
name = "kafka-transaction-probe"
version = "0.1.0"
edition = "2021"
rust-version = "1.95"
publish = false
license = "Apache-2.0"

# Keep the protocol probe out of LaminarDB's runtime workspace and dependency graph.
[workspace]

[dependencies]
futures-executor = "=0.3.32"
rdkafka = { version = "=0.39.0", default-features = false, features = ["cmake-build"] }
sha2 = "=0.10.9"
