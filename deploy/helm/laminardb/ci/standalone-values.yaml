replicaCount: 1

laminardb:
  mode: single
  logLevel: info
  consoleToken:
    existingSecret: laminardb-console

persistence:
  checkpoints:
    enabled: true
    size: 5Gi

resources:
  limits:
    cpu: "1"
    memory: 1Gi
  requests:
    cpu: 250m
    memory: 256Mi
